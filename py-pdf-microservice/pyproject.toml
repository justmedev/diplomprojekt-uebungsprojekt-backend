[project]
name = "py-pdf-microservice"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Ilja Busch", email = "ilja.ragnar@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.141.1",
]

[project.scripts]
py-pdf-microservice = "py_pdf_microservice:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
